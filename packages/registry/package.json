{
  "name": "@acpjs/registry",
  "version": "0.1.0",
  "description": "acpjs registry index fetch/cache, AgentDefinition resolution, and ensureInstalled.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@acpjs/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.2"
  },
  "engines": {
    "node": ">=24"
  }
}
