{
  "name": "@acpjs/protocol",
  "version": "0.6.0",
  "description": "acpjs normalized ACP-derived event/state model, pure reducer and host-client adapter types.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zhangyu1818/acpjs.git",
    "directory": "packages/protocol"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  },
  "engines": {
    "node": ">=24"
  }
}
